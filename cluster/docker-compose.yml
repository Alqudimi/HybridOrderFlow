# ===========================================================================
# Spark Standalone Cluster + MongoDB — Docker Compose
# ===========================================================================
#
# يشغّل كلاستر Spark كامل (Master + 2 Workers) مع قاعدة MongoDB،
# ويوفّر مجلداً مشتركاً (shared volume) لملفات البيانات والمشروع.
#
# التشغيل:
#   docker compose -f cluster/docker-compose.yml up -d
#
# الإيقاف:
#   docker compose -f cluster/docker-compose.yml down
#
# واجهة Spark Master UI:
#   http://localhost:8080
#
# ===========================================================================

services:
  # -----------------------------------------------------------------------
  # MongoDB — قاعدة البيانات المشتركة (يمكن الوصول إليها من جميع العقد)
  # -----------------------------------------------------------------------
  mongodb:
    image: mongo:7.0
    container_name: spark-cluster-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - spark-network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  # -----------------------------------------------------------------------
  # Spark Master — العقدة الرئيسية (spark://spark-master:7077)
  # -----------------------------------------------------------------------
  spark-master:
    image: apache/spark:3.5.0
    container_name: spark-master
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master
    environment:
      - SPARK_MASTER_HOST=spark-master
      - SPARK_MASTER_PORT=7077
      - SPARK_MASTER_WEBUI_PORT=8080
    ports:
      - "7077:7077"   # Spark Master port
      - "8080:8080"   # Spark Master Web UI
      - "4040:4040"   # Spark Application UI (Driver)
    volumes:
      - ../data:/opt/spark/pipeline-data:ro
      - ../jars:/opt/spark/pipeline-jars:ro
      - ../src:/opt/spark/pipeline-src:ro
      - ../config:/opt/spark/pipeline-config:ro
      - spark_logs:/opt/spark/logs
    networks:
      - spark-network
    depends_on:
      mongodb:
        condition: service_healthy

  # -----------------------------------------------------------------------
  # Spark Worker 1 — عقدة عمل أولى
  # -----------------------------------------------------------------------
  spark-worker-1:
    image: apache/spark:3.5.0
    container_name: spark-worker-1
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077
    environment:
      - SPARK_WORKER_CORES=2
      - SPARK_WORKER_MEMORY=2g
      - SPARK_WORKER_WEBUI_PORT=8081
    ports:
      - "8081:8081"   # Worker 1 Web UI
    volumes:
      - ../data:/opt/spark/pipeline-data:ro
      - ../jars:/opt/spark/pipeline-jars:ro
      - ../src:/opt/spark/pipeline-src:ro
      - ../config:/opt/spark/pipeline-config:ro
    networks:
      - spark-network
    depends_on:
      - spark-master

  # -----------------------------------------------------------------------
  # Spark Worker 2 — عقدة عمل ثانية
  # -----------------------------------------------------------------------
  spark-worker-2:
    image: apache/spark:3.5.0
    container_name: spark-worker-2
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077
    environment:
      - SPARK_WORKER_CORES=2
      - SPARK_WORKER_MEMORY=2g
      - SPARK_WORKER_WEBUI_PORT=8082
    ports:
      - "8082:8082"   # Worker 2 Web UI
    volumes:
      - ../data:/opt/spark/pipeline-data:ro
      - ../jars:/opt/spark/pipeline-jars:ro
      - ../src:/opt/spark/pipeline-src:ro
      - ../config:/opt/spark/pipeline-config:ro
    networks:
      - spark-network
    depends_on:
      - spark-master

networks:
  spark-network:
    driver: bridge

volumes:
  mongodb_data:
  spark_logs:
